const plusTen = (num: number): number => num + 10;
const makeHeart = (a: string, b: string): string => a + "❤" + b;
const makeArrayBool = (a: string, b: number): boolean[] => [!!a, !!b];

const test = (a: number, b: number): void => {
  const num = a + b;
};
